<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="code.fortomorrow.riceappadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_shop_24"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_shop_24"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminUserProductsActivity"></activity>
        <activity android:name=".AdminNewOrdersActivity" />
        <activity android:name=".AdminAddNewProductActivity" />
        <activity android:name=".NewRiceCategoryAdd" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>